import React from 'react'

export default function HintSmall() {
  return (
    <div className="tips">
    <div className="title">The keyboard is supported.</div>
    <div className="hint small">If it doesn't work, click on the page.</div>
    <div className="hint">For All Clean <span className="hint-sel">AC</span> use <span className="hint-btn">Delete</span> button.</div>
    <div className="hint">For Cancel Entry <span className="hint-sel">CE</span> use <span className="hint-btn">End</span> button.</div>
   </div>
  )
}
